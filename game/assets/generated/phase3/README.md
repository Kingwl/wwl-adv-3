# Phase 3 Generated Assets

第三期素材聚焦替换最高优先级的色块感来源：

- 地图 tileset：旧井入口和墓园长廊的地面、墙、入口和出口。
- 卡牌动态图标：21 张已实现卡各 4 帧小图标，用于手牌和奖励按钮。
- 玩家战斗动作：idle、attack、cast、guard、hurt、death。

所有 raw 图保留在 `raw/`，处理后的透明图集在 `processed/`，生成 prompt 保留在 `prompts/`。
